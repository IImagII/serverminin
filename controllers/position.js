///api/position/:category(GET)
export function getByCategoryId(req, res) {
   res.status(200).json({ login: false })
}
///api/position(POST)
export function createPosition(req, res) {
   res.status(200).json({ login: false })
}
///api/position/:id(PATCH)
export function removePosition(req, res) {
   res.status(200).json({ login: false })
}
///api/position/:id(DELETE)
export function updatePosition(req, res) {
   res.status(200).json({ login: false })
}
